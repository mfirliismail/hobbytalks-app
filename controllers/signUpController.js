const { UsersSchema } = require('../models')
const bcrypt = require('bcrypt')

module.exports = {
    signUp: async (req, res) => {
        try {
            const s
        } catch (error) {
            
        }
    }
}
